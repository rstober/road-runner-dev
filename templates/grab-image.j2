  
{% for bnode in all_nodes.physical_nodes %}
{% if bnode.hostname == node.hostname %}
- name: Display node details
  debug:
    msg: 
    - "hostname: {{ node.hostname }}"

- name: Display node details
  debug:
    msg:
    - "hostname: {{ bnode.hostname }}"
    - "category: {{ bnode.category.name }}"
    - "software image: {{ bnode.category.softwareImageProxy.parentSoftwareImage.name }}"

{% endif %}
{% endfor %}
